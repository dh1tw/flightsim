class Instrument {
    constructor(x, y, size) {
        this.x = x;
        this.y = y;
        this.size = size;
    }
    
    draw(ctx) {
        // Méthode à implémenter dans chaque instrument
    }
    
    update(data) {
        // Méthode à implémenter dans chaque instrument
    }
}
